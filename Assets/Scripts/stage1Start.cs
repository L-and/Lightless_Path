using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class stage1Start : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        StartScriptManager.Instance.stage1();
    }

}
